<?xml version="1.0" encoding="UTF-8"?>
<strings>
    <M500>[Language] - Metadata language is not defined and other language are declared and Main metadata language MUST NOT be defined in other language section.</M500>
    <alert.M500>
        <div>Check that the main metadata language is not defined and other languages (ie.
            gmd:locale) elements exist (see Advanced view / metadata section). <br/>
            <span class="validationReportSuggestion">Suggestions: Define main metadata language or
                remove other language definition.</span></div>
    </alert.M500>
    <report.M500>Main metadata language is:</report.M500>
    <alert.M501>
        <div>Declare the main metadata language only once (see Advanced view / metadata section). <br/>
            <span class="validationReportSuggestion">Suggestions: remove it from the other language
                section.</span></div>
    </alert.M501>
    <report.M501>No duplicate languages found.</report.M501>
</strings>
