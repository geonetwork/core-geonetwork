<profiles>
    <profile name="Developer" extends="Administrator"></profile>
    <profile name="Administrator" extends="UserAdmin,Monitor"></profile>
    <profile name="UserAdmin" extends="Reviewer"></profile>
    <profile name="Reviewer" extends="Editor"></profile>
    <profile name="Editor" extends="RegisteredUser"></profile>
    <profile name="RegisteredUser" extends="Guest"></profile>
    <profile name="Guest"></profile>
    <profile name="Monitor" extends="RegisteredUser"></profile>
</profiles>